import {exerciseCalculator} from './exerciseCalculator';



describe('exerciseCalculator',()=>{
    test('exerciseCalculator should be a function',()=>
    //bmiCalculator();
    expect(typeof exerciseCalculator).toBe('function'))
});
